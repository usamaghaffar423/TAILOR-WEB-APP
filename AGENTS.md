<!-- vibebug:start version=2 -->
## VibeBug workflow

When a VibeBug issue is assigned or `/vibe-bug-work` is invoked, use the repository skill `vibe-bug-worker`.

Use `vibebug memory context` for a bounded context pack. Do not preload the memory index or all memory files. Current code and tests override memory.

<!-- vibebug:end -->
